// Some Common Mistakes

// Undefined Variables
// Whether or not you use the var keyword to declare a variable has a consequence of what scope the variable has; 
// so it is always best to use the var keyword

// Case Sensitivity

var myName = "Jeremy";
If (myName == "jeremy")
	alert(myName.toUppercase());

